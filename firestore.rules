rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TruckLink Rules
    // Allow read and write for authenticated users and demo evaluation
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
